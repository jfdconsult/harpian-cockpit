# HQP API (FastAPI /v1) — backend compartilhado com o Terminal
NEXT_PUBLIC_HQP_API_URL=http://localhost:8080

# xri-serve — micro-serviço do engine harpian-xri (uvicorn xri.serve:app --port 8879)
# dados BRUTOS (turbulence/absorption/DY/pesos/validação/calibração) — só pro Cockpit interno.
NEXT_PUBLIC_XRI_API_URL=http://localhost:8879
